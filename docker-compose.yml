# Machine generated by `arb-deploy`. Do not version control.
version: '3'
networks:
  default:
    external:
      name: arb-network
services:
  arb:
    build:
      context: .
      dockerfile: arb.Dockerfile
    image: arb:latest